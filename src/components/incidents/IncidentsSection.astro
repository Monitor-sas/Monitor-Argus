---
import { t } from "lingui-for-astro/macro";
---

<section class="summary-section" aria-label={t`Incidents`}>
	<h2 class="section-title">{t`Incidents`}</h2>
	<div class="empty-card">
		<p class="empty-title">{t`No active incidents`}</p>
		<p class="empty-body">{t`Incidents are published here when they affect service availability.`}</p>
	</div>
</section>